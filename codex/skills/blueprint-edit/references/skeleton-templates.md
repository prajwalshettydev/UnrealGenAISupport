# Declarative Skeletons

Use these as high-level starting points when building a spec for `apply_blueprint_spec`:

- Entry event -> validation branch -> action chain
- Trigger overlap -> cast/interface -> gameplay action
- Input event -> lookup/selection -> target call

Keep the actual spec tied to live node and pin names discovered from MCP inspection.
